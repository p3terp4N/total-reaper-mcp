-- templates/transcription.lua — Session template
-- Implementation in Phase 3-4

local transcription = {}

function transcription.build(session_name, bpm, time_sig, key)
  -- Placeholder
end

return transcription
